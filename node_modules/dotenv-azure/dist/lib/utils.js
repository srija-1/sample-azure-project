"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.populateProcessEnv = exports.compact = exports.difference = void 0;
function difference(arrA, arrB) {
    return arrA.filter(a => !arrB.includes(a));
}
exports.difference = difference;
function compact(obj) {
    const result = {};
    Object.entries(obj).forEach(([key, val]) => {
        if (val) {
            result[key] = val;
        }
    });
    return result;
}
exports.compact = compact;
/**
 * Add variable if does not exist in process.env
 * @param variables - an object with keys and values
 */
function populateProcessEnv(variables) {
    Object.entries(variables).forEach(([key, val]) => key in process.env || (process.env[key] = val));
}
exports.populateProcessEnv = populateProcessEnv;
//# sourceMappingURL=utils.js.map